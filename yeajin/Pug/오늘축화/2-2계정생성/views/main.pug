extends index 
block content
  p 계정 만들기

  h2 아이디
    form 
      input.id(type="text" name="username")
  h2 비밀번호 
    form
      input.ps(type="password" name="password")
  h2 닉네임
    form
      input.nn(type="text", name="nickname") 